const searchBarContainer = document.getElementById("search-container");

searchBarContainer.addEventListener("click", () => {
  console.log("clicked");
  searchBarContainer.style.backgroundColor = "#ffffff";
});
